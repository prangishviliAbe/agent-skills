interface:
  display_name: "Premium Web Motion"
  short_description: "Purposeful, interruptible, profiled interface motion"
  default_prompt: "Use $premium-web-motion for this motion layer: write the motion inventory first with a named job per animation, define duration and easing tokens, keep to transform and opacity, design the reduced-motion path, and verify interruption and performance on a throttled profile."
